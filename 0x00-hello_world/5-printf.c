#include <stdio.h>

/**
 * main - prints with proper grammar, but the outcome is a piece of art,
 * Return: 0 if the code sucessed, else returns other number
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
