#include <stdio.h>

/**
 * main - prints single digits
 * Return: 0 is the code sucessed
 */

int main(void)
{
	int num;

	for (num = 0; num < 10; num++)
	{
		printf(num);
	}
	printf("\n");
	return (0);
}
